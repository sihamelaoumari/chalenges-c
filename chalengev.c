#include <stdio.h>
#include <stdlib.h>
int main(){
	float km ;
	float m;
	printf("la vitesse en km:");
	scanf("%f",&km);
	m = km*0.27778;
	printf("la vitesse en minut par seconde est:%f",m);
	


/* run this program using the console pauser or add your own getch, system("pause") or input loop */

	return 0;
}
